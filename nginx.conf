server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    location /api/status {
        proxy_pass http://localhost:3001/api/status;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}